# fix_plan.md
- [ ] 1. Create `src/client/api_key_command.rs` with `run_api_key_command` and `resolve_api_key`: New module implementing sync subprocess command execution via `SHELL` (same as bedrock.rs:206 pattern) and `resolve_api_key` orchestrator that checks static key → cache → command → cache-store. Import `super::access_token::{get_access_token, is_valid_access_token, set_access_token}` for caching, `crate::utils::SHELL` for shell detection, `chrono::Utc` for timestamps. Read `src/client/access_token.rs` (full file, 32 lines) and `src/utils/command.rs:17-23` (SHELL struct) first. Done: file exists, exports `pub fn resolve_api_key(...)`. Files: src/client/api_key_command.rs (new). Depends: []
- [ ] 2. Register module in `src/client/mod.rs`: Add `mod api_key_command;` after `mod access_token;` (line 1) and add `pub use self::api_key_command::resolve_api_key;` after the other `pub use` re-exports (line 13). Done: `cargo build` passes with the new module. Files: src/client/mod.rs:1-14. Depends: []
- [ ] 3. Verify core module compiles: Run `cargo build 2>&1 | head -50`. If errors, fix imports in api_key_command.rs. Done: build passes cleanly. Files: (verification only). Depends: [1, 2]
- [ ] 4. Add `api_key_command` and `api_key_command_expires_in` fields to `OpenAIConfig`: Add `pub api_key_command: Option<String>` and `pub api_key_command_expires_in: Option<u64>` after `organization_id` field (line 17). Follow pattern of existing `Option<String>` fields. Done: struct compiles. Files: src/client/openai.rs:12-22. Depends: [3]
- [ ] 5. Add `api_key_command` and `api_key_command_expires_in` fields to `OpenAICompatibleConfig`: Add fields after `api_key` (line 13). Done: struct compiles. Files: src/client/openai_compatible.rs:9-18. Depends: [3]
- [ ] 6. Add `api_key_command` and `api_key_command_expires_in` fields to `ClaudeConfig`: Add fields after `api_base` (line 15). Done: struct compiles. Files: src/client/claude.rs:12-21. Depends: [3]
- [ ] 7. Add `api_key_command` and `api_key_command_expires_in` fields to `GeminiConfig`: Add fields after `api_base` (line 15). Done: struct compiles. Files: src/client/gemini.rs:11-20. Depends: [3]
- [ ] 8. Add `api_key_command` and `api_key_command_expires_in` fields to `CohereConfig`: Add fields after `api_base` (line 15). Done: struct compiles. Files: src/client/cohere.rs:12-21. Depends: [3]
- [ ] 9. Add `api_key_command` and `api_key_command_expires_in` fields to `AzureOpenAIConfig`: Add fields after `api_key` (line 11). Done: struct compiles. Files: src/client/azure_openai.rs:7-16. Depends: [3]
- [ ] 10. Update OpenAI `prepare_chat_completions` and `prepare_embeddings` to use `resolve_api_key`: Replace `self_.get_api_key()?` at lines 47 and 67 with `resolve_api_key(self_.name(), self_.get_api_key(), self_.config.api_key_command.as_deref(), self_.config.api_key_command_expires_in)?`. Pattern reference: `self_.name()` works per macros.rs:155. Done: both call sites updated. Files: src/client/openai.rs:47,67. Depends: [4]
- [ ] 11. Update OpenAI-Compatible `prepare_chat_completions`, `prepare_embeddings`, `prepare_rerank` to use `resolve_api_key`: Replace `self_.get_api_key().ok()` at lines 42, 63, 80 with `resolve_api_key(self_.name(), self_.get_api_key(), self_.config.api_key_command.as_deref(), self_.config.api_key_command_expires_in).ok()`. Done: all 3 call sites updated. Files: src/client/openai_compatible.rs:42,63,80. Depends: [5]
- [ ] 12. Update Claude `prepare_chat_completions` to use `resolve_api_key`: Replace `self_.get_api_key()?` at line 46 with resolve_api_key call. Done: call site updated. Files: src/client/claude.rs:46. Depends: [6]
- [ ] 13. Update Gemini `prepare_chat_completions` and `prepare_embeddings` to use `resolve_api_key`: Replace `self_.get_api_key()?` at lines 44 and 71 with resolve_api_key calls. Done: both call sites updated. Files: src/client/gemini.rs:44,71. Depends: [7]
- [ ] 14. Update Cohere `prepare_chat_completions`, `prepare_embeddings`, and `prepare_rerank` to use `resolve_api_key`: Replace `self_.get_api_key()?` at lines 45, 66, 93 with resolve_api_key calls. Done: all 3 call sites updated. Files: src/client/cohere.rs:45,66,93. Depends: [8]
- [ ] 15. Update Azure OpenAI `prepare_chat_completions` and `prepare_embeddings` to use `resolve_api_key`: Replace `self_.get_api_key()?` at lines 48 and 68 with resolve_api_key calls. Done: both call sites updated. Files: src/client/azure_openai.rs:48,68. Depends: [9]
- [ ] 16. Full compilation and test gate: Run `cargo build && cargo test 2>&1 | tail -30`. All must pass with zero regressions. Done: build and tests pass. Files: (verification only). Depends: [10, 11, 12, 13, 14, 15]
- [ ] 17. Add `api_key_command` example to `config.example.yaml`: Insert commented example block after the openai-compatible section (~line 130) showing `api_key_command` and `api_key_command_expires_in` fields with explanatory comments. Reference format at line 97-130 for comment style. Done: example added. Files: config.example.yaml:120-140. Depends: [16]
- [ ] 18. Update user-facing documentation: Update `findings/fix-plan-iteration-20260616/user-facing-docs.md` with final accurate line numbers and any deviations from original plan discovered during implementation. Done: docs updated. Files: findings/fix-plan-iteration-20260616/user-facing-docs.md. Depends: [16]
